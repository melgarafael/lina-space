//! Spike **W1-2 — Slint**: renderiza 1 nó-terminal real (lina-pty + lina-vt) numa
//! janela Slint e mede o que der (frame timing, integração AccessKit, IME),
//! documentando esforço/obstáculos. Objetivo: dado para a decisão (W1-4), não produto.

fn main() {
    println!("spike-slint: TODO(W1-2) — janela Slint + nó-terminal real + medições");
}
