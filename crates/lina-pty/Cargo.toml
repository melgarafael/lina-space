[package]
name = "lina-pty"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
[dependencies]
# Onda 0: portable-pty do crates.io (Mac/Linux funcionam direto).
# TODO(W5-3): vendorizar read-only + patch dos 3 flags ConPTY + bundle
# conpty.dll/OpenConsole.exe no Windows. NAO bloqueia o Mac aqui.
portable-pty = "0.9.0"
thiserror.workspace = true
tracing.workspace = true

# kill graceful (SIGTERM -> SIGKILL com timeout) no unix; Windows (TerminateProcess) na W5-3.
[target."cfg(unix)".dependencies]
libc = "0.2.186"
