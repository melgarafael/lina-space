//! F3-5-6 · frente CONTEXTO (dono: Terminal I) — STUB da largada.
//!
//! Pistas: o usuário seleciona pastas/arquivos que a IA passa a enxergar num projeto →
//! `ClueSetDefined { scope, paths, label }`; projeção `ClueSet` (último por scope vence,
//! molde `CostLedger`). Uma camada do briefing lê isso e injeta os paths no contexto do
//! terminal daquele projeto (remover a pista → some no próximo turno). A fiação no
//! briefing vive em `briefing.rs` (também de I). `paths` é DADO, jamais autoridade.
//! Preencha aqui; o `pub mod` já está em lib.rs (não toque lib.rs).
