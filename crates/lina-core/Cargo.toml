[package]
name = "lina-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
lina-pty = { path = "../lina-pty" }
lina-vt = { path = "../lina-vt" }
lina-host = { path = "../lina-host" }
thiserror.workspace = true
anyhow.workspace = true
tracing.workspace = true
uuid.workspace = true
# W0-4: pub/sub in-process do Workspace Bus (broadcast usável sem runtime: send + try_recv).
tokio = { version = "1", features = ["sync"] }

[dev-dependencies]
serial_test = "3.4.0"
# TODO: rusqlite (event store), serde/serde_json (envelope JSON na fronteira) — W0-5.
