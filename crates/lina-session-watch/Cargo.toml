[package]
name = "lina-session-watch"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
thiserror.workspace = true
# F1-1-2: parser incremental de session-files JSONL + projeção SQLite (camada 3 de
# observabilidade). Versões locais alinhadas ao Cargo.lock (mesmo padrão de
# lina-core, que declara rusqlite/serde_json localmente).
serde_json = "1.0.150"
rusqlite = { version = "0.40.0", features = ["bundled"] }
