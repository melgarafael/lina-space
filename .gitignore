/target
**/*.rs.bk
Cargo.lock
.DS_Store
*.db
*.db-wal
*.db-shm
.lina/

# targets dos crates excluidos do workspace
app/*/target/
spikes/*/target/
**/Cargo.lock
!Cargo.lock

# artefatos efemeros da orquestracao Maestri (nao versionar)
.entrega-*.md
.crosscheck-*.md

# artefatos de empacotamento (Lina.app, .dmg) — gerados por packaging/make-app.sh
/dist/

# artefatos locais de bring-up/teste no Windows
.codex/
*.log
*.exit
/OpenConsole.exe
/conpty.dll

# Artefatos de runtime do Lina: a ficha/kit que o app gera no cwd de um agente.
# Quando o MAESTRO roda com cwd = a raiz do repo, estes vazam aqui. A FONTE canônica
# vive em assets/lina-doctrine/ (versionada). Ancorados na raiz (não afetam assets/).
/AGENTS.md
/GEMINI.md
/.claude/settings.json
/.claude/skills/lina-agent-bus/
# F1-4-7: chave privada de assinatura e lotes de chaves NUNCA entram no repo
*.lina-signing.private
chaves-*.csv
