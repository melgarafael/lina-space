
## Achados — rodada F3-3 (Maestro A, 2026-06-21)

- **[BUG] `lina history @colega` nega leitura cross para o Maestro** mesmo no mesmo Espaço: "leitura cross negada: 019eeb6f-bb4a NÃO pertence ao mesmo Espaço que o dono". O reader_node_id resolvido (bb4a) diverge do id do MESMO Terminal A como SENDER dos handoffs (bb55). `check`/`list`/`handoff`/`plan` funcionam; só `history` falha → o Maestro NÃO consegue "ver a tela do colega" (observabilidade ADR 0006 quebrada). Contornado lendo `events/log.jsonl` direto. Candidato a fix: reconciliar reader_node_id (multi-id do mesmo nó) no history.
- **[GARGALO de fluxo] workers param em `permission_prompt` LOCAL (AskUserQuestion/yn) em vez de perguntar ao Maestro via `lina ask`** → travam a orquestração autônoma. B perguntou "testar com terminais reais ou a prova basta?" na tela DELE (devia ter sido `lina ask @Maestro`); G um "Do you want to proceed?". O Maestro não tem verbo para responder prompt modal de worker (só o humano na UI ou auto-aprovação). Some o fluxo autônomo. Candidato: doutrina do worker "decisao de escopo -> pergunte ao Maestro via lina ask, NAO AskUserQuestion local" + auto-aprovacao de yn seguro no guard.
