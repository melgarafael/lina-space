# Decisões do Maestro — rodada r1 da execução F2 (2026-06-12)

1. **Costura dos tokens (pedido do Terminal C, F2-1-1): troca da fonte ADIADA para story atômica F2-1-1b** — "embedding + consumo + célula": embarcar os estáticos OFL (Plex/Fraunces/JetBrains Mono) no .app · aplicar os 3 diffs Menlo→token (`main.rs:416`, `attention_ui.rs:774`, `agent_modal.rs:2253`) · derivar `FONT_PX` do token (3 usos, vira fn) · **re-derivar `CELL_W`/`CELL_H` (`bridge.rs:2986`) para JetBrains Mono** (ou medir via text system no boot) · validar grid na tela. Motivo: aplicar o diff sem embedding = fallback silencioso de fonte + célula desalinhada — viola "a tela nunca mente" (fio 4 do épico). O aviso veio do próprio C (costura §1-2) — acatado integralmente.
2. **Pergunta do C (escalas no tema.json): NÃO entram** — concordo com a leitura dele: spacing/radius são estrutura do design system, não preferência do usuário; override por token é o desenho da F2-1-4. Tipografia (famílias) e movimento (reduzir) no JSON estão certos — são preferências.
3. **Costura da sonda (pedido do Terminal E, F2-0-1): APLICADA nesta rodada** — variante `ProfWindowMetric` no core (`events.rs`, aditiva, braço META) + carimbo de scale factor e drenagem de summaries→evento no fecho de frame do `main.rs`. Validação de fora: core 405/0 · app 411/0 · clippy -D warnings EXIT=0 (exits diretos).
4. **Screenshot-de-glifo LoDPI (proposta do E): story própria** se a sessão de tela provar necessidade — por ora o carimbo de scale factor + aviso <1.5x cobre o gap §II.8 da onda V.
5. **F2-1-5 (catraca):** despachar na r2 (base de tokens agora existe); dono sugerido: Terminal E ou D.
