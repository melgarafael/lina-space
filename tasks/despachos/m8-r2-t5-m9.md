# Despacho M8-T5 — M9 Criar Espaço (Galeria de Focos + Diretório de Trabalho) · rodada 2

> Rodada M8-r2 · 2026-06-11 · Maestro externo → Terminal #41 (UIUX_DESIGNER)
> Pré-requisito NO CHÃO antes de começar: `.entrega-m8-t3.md` (create_workspace/validate_workdir)
> com `PRONTO:`. Leia primeiro — os contratos finais estão lá.
> Marque início: `echo INICIADO > "/Users/rafaelmelgaco/einstein workspace/lina-space/.iniciado-m8-t5"`

## CONTEXTO
Decisão do fundador (2026-06-09): M9 = Galeria de Focos COM seletor de diretório embutido — NÃO o
modal simples do Maestri. Spec congelada: `tasks/epico-f1/spec-m8-m9-fiacao.md` §2 (composição,
pré-preenchimento, validação CEDO, strings de erro leigas), §3 (4 vitrines de upsell free=1 — tier
nesta fatia é STUB `Pro`, então as vitrines ficam fiadas e dormentes) e §4 (a11y M9). Strings da
copy transcritas em `/tmp/mapas-m8/gallery-m9.md`. Padrão de modal do app (AgentModal/M6 — estado,
teclado, precedência): `/tmp/mapas-m8/ui-render.md` §3.

## FUNÇÃO
Designer-dev gpui: modal novo seguindo o padrão M6, reusando a Galeria de Focos existente.

## DIRECIONAMENTO
1. `cd "/Users/rafaelmelgaco/einstein workspace/lina-space"`.
2. Em `app/lina-gpui/src/gallery.rs` (a galeria é sua nesta rodada), crie o modo M9:
   - `pub struct CreateSpaceModal { pub preset_idx: usize, pub name: String, pub name_touched:
     bool, pub workdir: String, pub workdir_touched: bool, pub error: Option<WorkdirErrorView>,
     pub blocked: Option<String> }` — estado headless testável.
   - Cards de Foco: reusa `FocusPreset`/`preset_team()` (3 presets atuais — os 5 do ux-flows são
     backlog §6-B4, NÃO invente rótulos novos).
   - Nome pré-preenchido pelo Foco; duplicado → sufixo « (2)» + aviso leve inline (padrão M6, não
     bloqueia). Campo «Diretório de Trabalho» (rótulo LITERAL do fundador): pré-preenche
     `Settings.default_cwd` se definido, senão `~/EspaçosDeTrabalho/{nome}`; acompanha o nome
     ENQUANTO o usuário não tocou no campo (depois, nunca sobrescreve — flag `workdir_touched`).
   - `[ Procurar… ]` → seletor nativo (mesmo mecanismo do M6).
   - Validação na seleção E na criação via `workspace_boot::validate_workdir` (T3) — erros com as
     3 strings leigas EXATAS da spec §2; pasta inexistente NÃO é erro.
   - Criar = chama `workspace_boot::create_workspace(...)` (T3) e devolve o ws_root ao caller via
     callback `on_created(ws_root)` — o FOCO/troca é da integração (Maestro), não do modal.
   - Tier: receba `tier: LicenseTier` do caller; `Blocked` → card de upsell com as strings §1a
     («Você já usa o Espaço do plano Free (1 de 1)» + `[ Conhecer o PRO ]`) — fiado, dormente com
     o stub Pro.
3. Teclado/a11y §4: foco inicial no 1º card; tab cards → Nome → Diretório → Procurar → Criar →
   Cancelar; Enter SUBMETE sempre (a precedência da Fila vale só p/ ⌘⏎ — correção da banca);
   Esc fecha sem criar; erros anunciados ao focar o campo.
4. TDD headless: pré-preenchimento segue nome até tocar, sufixo de duplicado, blocked com tier
   Free renderiza vitrine (estado), erro de workdir mapeia para a string certa.

## FRONTEIRA DE ARQUIVOS (exclusiva sua nesta rodada)
- `app/lina-gpui/src/gallery.rs`. Mais NADA. A fiação no main.rs (abrir o modal a partir da
  sidebar/⌘, atalho, troca pós-criação) é do MAESTRO na integração — declare o contrato na entrega.
- **NÃO COMMITE.**

## OBJETIVO
M9 completo no padrão do app: suíte verde + clippy `-D warnings` + fmt EXIT=0 (exits diretos),
estado 100% testado headless, strings 100% citadas da copy.

## RESULTADO ESPERADO
`.entrega-m8-t5.md`: assinaturas públicas, contrato de integração com o main, strings usadas (§),
evidência dos exits + testes novos. Termine com `PRONTO:` ou `BLOCKED: <motivo>`.
