# Achados de DOGFOODING — sessão 2026-06-10 (desenvolvendo o Lina DENTRO do Lina)

> Auto-percepção: bugs/atritos vividos pelo próprio time de IA usando o produto. Cada item vira
> fix ou story. Status atualizado conforme a sessão avança.

| # | Achado | Evidência | Severidade | Status |
|---|---|---|---|---|
| 1 | **Identidade por cwd colide**: 9 nós spawnados com o MESMO cwd (repo) → `<cwd>/.lina/bootstrap.json` reescrito a cada admissão; whoami/handshake/outbox do MAESTRO viraram "Automatizador". F1-4-1 (`default_cwd` por Espaço) tornaria isso o DEFAULT do produto. | event log seq 20133-20184 (cwd idêntico) + `bridge.rs:3141` + `bin/lina.rs:26`; vivido pelo Maestro no turno 0 | ALTA (correção de identidade; autorização não afetada — duas camadas intactas) | **em fix** (Bug Finder, r1: env `LINA_NODE_ID/NAME` no spawn + ADR 0026) |
| 2 | **Bootstrap do papel MAESTRO instrui carregar skill estrangeira** `maestri-orchestrator` — que o guard do próprio Lina bloqueia. Instrução contraditória no turno 0. | SessionStart hook da sessão + erro do guard ao invocar a skill | MEDIA (UX do agente; turno 0 confuso) | **em fix** (Bug Finder, r1: papel MAESTRO → `lina-orchestration`) |
| 3 | **Papéis inconsistentes por superfície**: AUTOMATOR vira "DEVELOPER" no whoami; FRONTEND idem na lista de colegas; "reviewer" minúsculo no agents.json. | saídas de whoami/handshake/list da sessão | BAIXA-MEDIA | **em fix** (Bug Finder, r1: canonização + teste de paridade) |
| 4 | **`lina check` exibe UUID cru na superfície**: "ultima atividade A2A: handoff de 019eb26f-9190-… para @X" — deveria resolver para `@Nome` (zero jargão, inv#6). | saída do `lina check` desta sessão (Maestro) | BAIXA | aberto → rodada 2 |
| 5 | **Positivo (registro honesto):** despacho de 7 handoffs via `lina handoff` → 7/7 entregues e iniciados sem re-despacho (marcadores 14:08). A injeção faseada da F1-0 + retenção not-ready eliminaram a intermitência que o protocolo Maestri sofria (lição §4.4 do handoff ficou OBSOLETA para o Lina). | `.iniciado-*` 7/7 + `lina check` Busy 7/7 | — | evidência p/ o gate F1-0/F1-3 (uso real) |
| 6 | **`lina spawn` não normaliza o sigil `@`**: o nó nasceu NOMEADO "@Especialista em Licenças" (com @ embutido) — o @ é endereçamento, não nome; superfícies exibem "@@" em endereços compostos. | `lina list` + `TerminalSpawned` seq 20326 | BAIXA | aberto → rodada 2 |
| 7 | **`TerminalSpawned.cli` recebe o NOME do nó em vez do CLI** no caminho do spawn: nós do ⌘N registram `cli:"Claude Code"`; o spawnado registrou `cli:"@Especialista em Licenças"`. Polui projeções por-CLI (detecção/perfis/custo). | event log seq 20326 vs 20134 | MEDIA | aberto → rodada 2 |
| 8 | **Positivo:** spawn físico de ponta a ponta em uso real — `SpawnRequested`→`TerminalSpawned` (cwd virgem `n-<uuid>`, ADR 0022 ✓)→`SpawnAdmitted`→1º prompt na fila→nó `Running` no roster. O seam da F1-3 (rodada `d59283b`) FUNCIONA. | event log seq 20324-20329 | — | evidência p/ roteiro de tela do fundador |
| 9 | **1º prompt do spawn não contextualiza o cwd**: o spawnado nasce em dir gerenciado próprio, mas nada o avisa de onde está — prompt com caminho relativo do orquestrador quebra silenciosamente. Candidata: ficha do spawnado citar cwd + o orquestrador ser instruído (skill lina-spawn-terminal) a usar caminhos ABSOLUTOS. | esta sessão (correção manual via lina ask) | BAIXA | **parcial `63d4e6b`** (aviso na description da skill); ficha citar cwd → rodada futura |
| 10 | **Skills NÃO carregavam no Codex** — `invalid description: exceeds maximum length of 1024 characters`: TODAS as 11 descriptions estouravam (2992-6642 chars). A portabilidade 3-CLI (critério F1-3-3) estava QUEBRADA no Codex real e ninguém via (o Claude aceita descriptions longas). | transcript real `/tmp/lina-3cli/codex-transcript.txt` (codex-cli 0.138) | MEDIA | **FECHADO `63d4e6b`** (descriptions 719-851 chars + teste guardião `descriptions_fit_codex_limit_of_1024_chars`) |
| 11 | **Limites de conta derrubam o time inteiro sem aviso estruturado**: o limite mensal do claude.ai travou os 9 terminais NO MEIO da rodada (2 fatias morreram sem entrega); o Lina não detecta/sinaliza "terminal travado por limite de conta" — aparece como Idle comum. Candidata de produto: detectar padrão de erro de limite no grid (mesma família do CliDetector/attention) → badge honesto "sem créditos" + fila de atenção. | sessão 2026-06-10 ~15h (Arquiteto e Licenças morreram mudos) | MEDIA (produto) | aberto → candidata F1-6/F2 |

## Continuação — sessão 2026-06-11 (retomada da r3 após reabrir o app)

| # | Achado | Evidência | Severidade | Status |
|---|---|---|---|---|
| 12 | **`lina spawn --prompt` NÃO entrega o 1º prompt ao terminal recém-nascido.** O verbo responde "o 1o prompt ja vai na fila dele" (APROVADO), o nó nasce, roda só o bootstrap (~656 tokens, `NodeStatusChanged status:Idle from:Running reason:end_of_response`) e fica OCIOSO — nunca executa o despacho. Os 6 terminais da r3 (bugfinder/arquiteto/ia/telas/revisor/designer) nasceram assim; ZERO marcadores `.iniciado-*` em qualquer cwd. O dead-letter guarda um `msg_spawn1st-019eb2b3-…json` (o "primeiro prompt" cai em dead-letter). Bloqueia a orquestração automática por spawn — o coração do produto ("o time coopera sem fios"). | event log seq 20812-20865 (NodeAdded→RoleAssigned→TokenUsage~656→Idle/end_of_response, 6 nós); `find .iniciado-*` vazio em repo/$HOME/$LINA_HOME; `dead-letter/msg_spawn1st-*.json` | **ALTA** | aberto → fix de produto (entrega do 1º prompt do spawn) |
| 13 | **`lina handoff` para terminal recém-spawnado fica preso em `outbox/.inflight/` (nunca entregue).** Handoff respondeu "ainda SEM confirmacao de entrega"; a msg `msg_019eb721-…` parou em `.inflight/` e NÃO gerou `MessageDelivered` nem evento no log. O `.inflight/` ainda acumula msgs das 11:14 (sessão anterior) que nunca drenaram → fila travada / head-of-line blocking. Agravante: `lina check "@Bug Finder"`/roteamento resolve o nome para um node MORTO de sessão antiga (`019eb26f…`) em vez do vivo no roster (`019eb71d…`) — reuso de nome entre sessões casa com o lifecycle antigo. | `outbox/.inflight/msg_019eb721-…json` presa; `grep 019eb721 log.jsonl` vazio; `.inflight/` com msg_019eb709 (11:14) presas; `lina check` apontando ts 1781187785668 (node 019eb26f morto) | **ALTA** | aberto → fix de produto (drenagem da fila + resolução nome→nó vivo) |
| 14 | **`lina check` reincide no UUID cru + resolve nome ambíguo para nó morto** (reforça o achado #4): além de exibir UUID na superfície, escolhe o lifecycle ERRADO quando o nome foi reusado. | `lina check "@Bug Finder"` desta sessão | BAIXA-MEDIA | aberto (junto do #4 e #13) |
| 16 | **Pump de entrega A2A morto após reabrir o app**: desde o boot 11:26 NENHUMA mensagem do `.inflight` foi entregue (zero `MessageRouted`/`Delivered`/`Retained` no log para as msgs novas); quarentenar as órfãs do "Automatizador" não destravou — não é head-of-line, o pump não drena. Os 6 spawns + 2 handoffs ficaram presos. CAUSA PROVÁVEL a investigar no código: recovery do `.inflight` pós-restart não re-semeia o motor, ou o pump não tickeia nesta instância. | `.inflight/` com 8 msgs paradas ≥30min; log sem eventos de entrega pós-boot | **ALTA** (bloqueia orquestração) | aberto → fix de produto |
| 19 | **Turno REAL de 24 min observado** (Bug Finder, seq 20881→20917) **> teto de retenção 600s** → 1º prompt de spawn foi à DLQ. Dado para o gatilho de revisita do ADR 0020 (p95×profundidade+margem, quando o dashboard F1-1-5 der tempos por agente). Consideração de produto: 1º prompt de spawn talvez mereça classe de retenção própria. | arqueologia r4 do Core A2A (`.entrega-a2a-spawn.md`) | MEDIA (produto/calibração) | aberto → revisita ADR 0020 |
| 12b | **Atualização dos #12/#13/#16 (r4):** re-derivação no HEAD provou que o "pump morto" era o FREIO restaurado corretamente (feature) + `Queued` invisível; os bugs reais eram (a) spawn furava o freio (`router.rs:758` FECHADO), (b) `msg_spawn1st-*` quebrava o FIFO do drain (`mailbox.rs:612` FECHADO), (c) `lina check` resolvia nome p/ nó morto (`resolve_check_node` FECHADO). 5 testes RED→GREEN. | `.entrega-a2a-spawn.md` | — | **#12/#13/#16 FECHADOS na r4** |
| 18 | **Família de testes descoberta-de-binário flakeia sob carga multi-worker** (3 ocorrências na r4, sempre timeout de `query_version` com CPU saturada por compilação paralela de outro worker): `cli_discovery::discover_resolves_version_of_present_cli` (core), `dev_tools::discovery_maps_binary_to_logical_id` e `onboarding::run_install_then_redetect_finds_binary` (app). Todos passam isolados em <0,5s. Custo real: o Maestro re-deriva 1 a 1 a cada validação de fora. Candidata: timeout do `query_version` em testes proporcional à carga, ou marcador `serial`/retry nos 3. | runs r4 do Maestro: `/tmp/v-ws.log`, `/tmp/v-app.log` vs isolados verdes | BAIXA-MEDIA (custo de validação, não defeito de produto) | aberto → candidata de harness |
| 17 | **`lina spawn` deriva `from`/autonomia do `.lina/bootstrap.json` do CWD** (resquício pré-ADR-0026): rodado fora do dir que tem o arquivo → erro "nao foi possivel ler .lina/bootstrap.json"; pior, num Espaço com Diretório de Trabalho COMPARTILHADO (default do M9 pós-`f1d4810`) o arquivo é de QUEM ESCREVEU POR ÚLTIMO → o `from` do spawn sai com identidade de OUTRO terminal. Os demais verbos já usam `LINA_NODE_ID/NAME` do env (ADR 0026); o spawn ficou para trás. | sessão 2026-06-11 22h (Maestro/Terminal A): spawn falhou em subdir; `.lina/bootstrap.json` do repo continha `terminal_name: "Terminal D - effort: medium"` | MEDIA-ALTA (identidade/atribuição no caminho mais sensível — spawn) | aberto → candidata r4/r5 (alinhar spawn ao ADR 0026) |
| 15 | **O orquestrador é CEGO ao conteúdo dos colegas** (apontado pelo fundador): para diagnosticar "o terminal recebeu a tarefa?" o Maestro teve que PERGUNTAR ao humano o que aparecia na tela — `lina check`/`lina list` só dão status do roster (Idle/Busy), não o que está NA TELA/scrollback do colega. O orquestrador deveria inspecionar o último output/estado-de-tela de um terminal por si só (é a fiação do F1-5-8 `lina history`/leitura cross-terminal — existe a API core na r3, falta o verbo/uso pelo Maestro). Sem isso, monitorar trajeto (passo 6 do loop) depende do humano. | esta sessão: fundador respondeu "voce deveria ser capaz de ver"; `lina check` só devolve status+última-A2A | **MEDIA-ALTA** (observabilidade do orquestrador) | aberto → fiar F1-5-8 ao alcance do Maestro |

## Continuação — sessão 2026-06-12 (kickoff da pesquisa F2, Maestro = Terminal B)

| # | Achado | Evidência | Severidade | Status |
|---|---|---|---|---|
| 20 | **Não existe verbo de ESCRITA no plano compartilhado** (`lina plan` = só `read | claim | check`): a doutrina manda o orquestrador "escrever os itens no plan.md com parents:" e proíbe editar `.lina/plan.md` na mão — mas nenhum verbo cria item. 2ª ocorrência (a 1ª foi o `lina plan add` fantasma pego pelo guardião na F1-3). O Maestro da pesquisa F2 caiu no mesmo buraco: workspace sem plano e sem como semear um. Workaround: distribuição por despachos ricos em `tasks/` + handoff (padrão F1). Candidata de produto: `lina plan add/seed` (escritor único = app). | `lina plan --help` desta sessão (verbos: read/claim/check); `lina plan read` = "(sem plano ainda)" | **MEDIA-ALTA** (coordenação: o board não conta a história sem itens) | aberto → candidata F2/F3 |
| 21 | **`lina vault search` pendura sem output nem timeout** (Terminal D, pesquisa D0): 3 buscas encadeadas ("Reaction Cards", "SUS usabilidade", "teste de 5 segundos") ficaram >2 min sem emitir 1 byte — processo vivo, zero resultado, zero erro; precisou de kill manual. O caminho determinístico (`grep` no `.lina/vault-index/*.md`, ~38k linhas) respondeu instantâneo na mesma sessão. Vault grande (Obsidian inteiro) é o cenário real do usuário. Candidata: timeout interno + fallback pro índice PageIndex (que já existe). | task `br4s41c21` desta sessão (output vazio, kill manual) vs grep no índice <1s | MEDIA (verbo de contexto inutilizável em vault grande; agente fica cego ou trava o turno) | aberto → candidata F2/F3 |
| 22 | **Bootstrap imprime caminho de vault DESATUALIZADO**: o SessionStart do Terminal C (pesquisa D1, 2026-06-12) imprimiu `VAULT OBSIDIAN: ~/Library/Application Support/Lina/Lina/vault`, mas o vault real (confirmado por `lina vault path`) é `~/Documents/Obsidian Vault`. O caminho do bootstrap nem existe como vault. Agente que confiar no bootstrap sem cruzar com `lina vault path` lê pasta errada/vazia e conclui "vault sem conteúdo". Candidata: bootstrap ler de `.lina/vault.json` (mesma fonte do verbo) em vez de template estático. | Sessão D1 do Terminal C: bootstrap vs `lina vault path` divergentes | MEDIA (contexto do segundo cérebro silenciosamente quebrado pra quem confia no bootstrap) | aberto → candidata F2 |
| 23 | **`lina ask --intent status` sem confirmação de entrega**: reporte de início da D1 ao Maestro retornou "ok: enviada... ainda SEM confirmacao de entrega apos a espera (o Espaco pode estar ocupado)" — o remetente não sabe se o status chegou, e a orientação ("confirme com lina list e tente de novo") empurra retry manual pro agente, gerando risco de reporte duplicado. Para `--intent status` (fire-and-forget auditável por definição), a entrega deveria ser garantida pela fila/event log sem o remetente re-tentar. | Sessão D1 do Terminal C, envio de status 09:2x (id msg_019ebbca-a5e0-...) | BAIXA-MEDIA (status reports são o tecido da observabilidade do time; semântica de retry ambígua) | aberto |
| 21 | **Terminal preso em `Blocked (circuit_breaker)` sem caminho de recuperação legível**: o Core A2A armou o breaker logo após receber handoff (2 entregas da mesma tarefa em ~3min — possível causa: dedupe/limite por item) e ficou >1h preso; o fundador olhou a tela e concluiu "já concluiu" (estado ilegível para humano), enquanto `lina check` só diz `circuit_breaker` sem dizer O QUE armou nem COMO soltar. Worker perdido + leitura humana errada = dupla falha de superfície. Candidata F2 (legibilidade do estado no card: "pausado por segurança — clique para liberar") + investigar causa no router. | sessão 2026-06-12: `lina check "@Core A2A"` = Blocked/circuit_breaker por >1h; D4 realocada ao Terminal C | **MEDIA-ALTA** (orquestração + UX de estado) | aberto |
| 22 | **PADRÃO RECORRENTE (2× na mesma sessão): o 1º handoff a um terminal ocioso-há-muito é consumido sem ação** — Terminal E (D3) e Red Team (onda V) receberam o handoff (entrega confirmada, `ultima atividade A2A` registrada), transicionaram para `Idle (end_of_response)` e NUNCA começaram nem reportaram; a tentativa 2 idêntica funcionou de primeira nos dois casos. Hipótese a investigar: prompt injetado aterrissa num estado do CLI que o engole (contexto comprimido? turno fantasma?) — distinto do achado #12 (spawn) pois aqui o terminal É vivo e a entrega É confirmada. Workaround do orquestrador: confirmar 1º evento de progresso e re-despachar 1×. Candidata: detectar "delivery→end_of_response sem DomainEvent novo em N min" como projeção de alerta (ADR 0019 já tem a definição de progresso — falta o alarme nesse caso específico). | sessão 2026-06-12: msgs 019ebbc9-9e88 (E, engolida) → re-envio ok; 019ebbfc-9be9 (Red Team, engolida) → re-envio em curso | **MEDIA-ALTA** (confiabilidade da orquestração) | aberto |
| 22b | **Atualização do #22:** o Red Team engoliu DOIS despachos consecutivos (msgs 019ebbfc-9be9 e 019ebc02-5e5e — entrega confirmada, `Idle/end_of_response`, zero trabalho/reporte). Breaker do orquestrador respeitado: sem 3ª tentativa; onda V reroteada com verificação cruzada (Terminal D ← D1-D4; Terminal E ← D0). O padrão agora tem 3 ocorrências em 2 terminais distintos na mesma sessão — sobe a prioridade da investigação (delivery→consumo sem turno de trabalho). | sessão 2026-06-12 | **ALTA** (3ª ocorrência) | aberto |
| 21c | **Atualização do #21 (vault search pendura):** reincidência na F2-0-D (Terminal C) com evidência mais forte — `lina vault search "roteiro da tela"` ficou **50min57s** pendurado (medido via `ps -o etime`, pid 85700), processo vivo, ZERO bytes de output, até kill manual. Não é lentidão transitória: é hang aberto. Confirma a candidata do #21 (timeout interno + fallback pro índice PageIndex — o caminho determinístico respondeu instantâneo de novo nesta sessão; o arquivo procurado foi achado em <1s via `git show --stat`). | sessão F2-0-D 2026-06-12: task `b34o6zy2c`, etime 50:57, output vazio | MEDIA→**MEDIA-ALTA** (2 terminais, 4 queries penduradas na mesma sessão) | aberto |
| 24 | **Browser do Playwright MCP é UM só para o Espaço inteiro — sessões de workers paralelos colidem**: durante a verificação visual do protótipo T1 (F2-0-D), um screenshot do seletor `.rail` capturou a página do T4 (de outro worker) — um colega navegou o browser compartilhado entre o meu `navigate` e o meu `screenshot`. Verificação visual de um worker pode validar/reprovar o artefato ERRADO em silêncio (a colisão só foi notada porque as peles dos territórios diferem muito). Candidata: 1 contexto/aba de browser por terminal (isolamento por sessão), ou ao menos o orquestrador serializar o uso em rodadas multi-worker. | sessão F2-0-D 2026-06-12: screenshots `#caixa`/`#redator`/`#construtor` = T1 correto; screenshot `.rail` minutos depois = rail do T4 com pill "Protótipo T4" | MEDIA (verificação visual multi-worker não confiável) | aberto |
| 23 | **Variante do #22: report de "comecei" consome o turno e o terminal volta a Idle sem trabalhar** — Terminal E reportou início da F2-0-1 e caiu em `Idle (end_of_response)` sem tocar `prof.rs`; o ask de status vira o turno inteiro. Distinto do #22 (lá a msg era engolida; aqui ela é respondida e o trabalho não segue). Sugere que o despacho deveria instruir "reporte E CONTINUE no mesmo turno" — e o produto poderia detectar "report-sem-DomainEvent-subsequente". Também: Terminal D reportou início e consta `Dead (app_reopened)` — ou lifecycle aponta nó antigo (reuso de nome, eco do #13/#14) ou morreu pós-report. | sessão 2026-06-12 ~13h40: checks pós-alerta da vigia r1 | MEDIA-ALTA (confiabilidade) | aberto |
| 17b | **Ampliação do #17: não é só o `spawn` — `lina ask` TAMBÉM deriva o `from` do `.lina/bootstrap.json` do CWD.** Rodado de `app/lina-gpui/` (cwd legítimo de quem acabou de validar com cargo), `lina ask ... --intent status` falhou com "nao foi possivel ler .lina/bootstrap.json (de onde vem o 'from')". Contradiz a nota do #17 ("os demais verbos já usam LINA_NODE_ID/NAME do env — ADR 0026"): ou o ask regrediu, ou nunca migrou. Efeito prático: todo worker que reporta status no meio de um build (cwd dentro do crate) perde o reporte; workaround = sempre reportar da raiz. | sessão 2026-06-12 ~15h (Terminal D, r2): exit 1 com a msg acima; mesmo comando da raiz = entregue ok | MEDIA-ALTA (mesma família do #17 — identidade por cwd no caminho de A2A) | aberto |
| 23c | **Atualização do #23 (metade "Terminal D consta Dead"): hipótese DESAMBIGUADA — é resolução nome→nó antigo, NÃO morte pós-report.** O próprio Terminal D, VIVO e no meio da entrega da r1 (F2-0-2/F2-0-3 entregues nesta sessão), observou: `lina list` o mostra como `Blocked` enquanto `lina check "@Terminal D - effort: medium"` devolve `Dead (app_reopened)` com última atividade A2A de nó antigo — duas superfícies do roster com leituras divergentes do MESMO nome, e o check resolvendo para nó de sessão anterior (eco direto do #13, `resolve_check_node` foi corrigido na r4 só para o caminho do check?). Implicação de orquestração: o Maestro que confiar no check/list pode re-despachar trabalho de um worker que está TRABALHANDO. | sessão 2026-06-12 ~14h30 (Terminal D, auto-observação durante r1): `lina list` = Blocked; `lina check` = Dead/app_reopened; entrega da rodada concluída em paralelo | **MEDIA-ALTA** (observabilidade mente para o orquestrador) | aberto |
| 23b | **4ª ocorrência da família #22/#23 (agora com Busy improdutivo):** Terminal A consumiu o handoff dos ADRs (r1 da execução F2), transicionou a Busy (vigia registrou), voltou a Idle ~1h30 depois com `docs/adr/` INTOCADO e zero reporte. Diferente do #22 (engolido sem Busy) e do #23 (report consome turno): aqui houve trabalho aparente sem artefato — possível turno gasto em leitura/contexto sem escrita, ou saída descartada. Re-despacho informado (tentativa 2) enviado com instrução anti-#23 explícita. A família #22/#23 agora cobre: engolido · report-consome-turno · busy-improdutivo — três modos do mesmo sintoma (1º despacho não vira artefato). | sessão 2026-06-12 ~14h30: monitor bm8mnk7xj + git status limpo em docs/adr/ | **ALTA** (família consolidada) | aberto |

## Achado (2026-06-12, Terminal A — selo F2-0-4/F2-0-5)
- **`lina vault search` não retorna (2 tentativas).** `lina vault search "F2-0-4 selar ADR 0028"` e `lina vault search "Epico Fase 2"` ficaram penduradas sem output (caíram em background por timeout do shell); contornei com `find` direto no vault (a nota `38 - Epico Fase 2.md` existe e é encontrável por nome). Não bloqueou, mas o verbo de busca do vault — o caminho que a doutrina manda o agente usar ANTES de assumir — está mudo nesta máquina/sessão. Vale investigar se é indexação ausente, termo com hífen, ou hang real do comando.

## Achado (2026-06-12, Terminal C — F2-2-1 catálogo de componentes)
- **A catraca de tokens (F2-1-5) conta `FontWeight::` / `px(<lit>)` até em COMENTÁRIO.** `debt_of_source` (token_ratchet.rs) varre `content.lines()` por substring, sem stripar comentário. Um doc-comment meu que DIZIA "zero literal `FontWeight::`" criou dívida `FontWeight:: 0→1` no arquivo, falhando o gate ironicamente. Workaround: nunca escrever a substring literal num comentário (parafrasear). Candidata: stripar comentários `//`/`//!`/`/* */` antes de contar, OU documentar a pegadinha no doc-comment do teste. BAIXA (some com disciplina), mas pega quem documenta o componente honestamente. | sessão F2-2-1: gate acusou `button.rs: FontWeight:: subiu 0 → 1` por causa do doc-comment da linha 7 | aberto
- **Testes de install/discovery (dev_tools/onboarding) são flaky sob carga paralela** — `silent_install_then_discovery_finds_tool`, `run_install_then_redetect_finds_binary`, `discovery_maps_binary_to_logical_id` spawnam subprocessos/PTYs reais com suposição de timing; sob `cargo test` paralelo durante carga (build concorrente), falham 2–3 não-deterministicamente, mas passam **isolados** (0.26s) e **serial** (439/0). Um worker pode concluir falsamente que sua mudança regrediu (perdi tempo re-provando que meu diff render-only não tocava install). Candidata: serializar os testes que spawnam subprocesso (mutex de teste) ou marcá-los `#[ignore]`-por-padrão + suite dedicada. MEDIA (falso-positivo de regressão custa o turno do worker; o gate paralelo do Maestro pode piscar vermelho sem causa real). | sessão F2-2-1: 0/3/2/2 falhas em rodadas paralelas idênticas; isolado e serial verdes | aberto
| 24 | **Coordenação por mensagens assíncronas + árvore compartilhada viva = o orquestrador pode ler estado INTERMEDIÁRIO como definitivo.** Sequência real: Maestro instrui promoção de módulo (E→depois→C); E entrega um reporte descrevendo o estado PRÉ-promoção e continua executando; Maestro lê a árvore no meio (build verde por coincidência do estado intermediário), emite CONTRAORDEM ao C; minutos depois a árvore muda (E completou) e a contraordem virou o erro. Custo: 2 mensagens de correção + ~10min de árvore vermelha. Lições de produto: (a) reporte de PRONTO deveria carimbar o ESTADO da árvore (ex.: hash dos arquivos da fronteira) para o orquestrador detectar drift relato-vs-disco; (b) instrução de 2 fases entre 2 workers merece um marcador explícito de fase concluída (não inferência por grep). Família nova: observabilidade de coordenação (parente do #15). | sessão 2026-06-12 ~17h (r5, promoção a11y_live) | MEDIA (coordenação) | aberto |
| 25 | **DISCO CHEIO derrubou a orquestração inteira (ENOSPC)** — 99%/159MB livres no meio da r5: nem o orquestrador conseguia executar comandos (cada execução grava arquivo temporário). Causa dominante NOSSA: ~30GB de target/ de cargo (21GB workspace + 8,4GB app) acumulados pelas rodadas multi-terminal na árvore compartilhada. Mitigação na hora: parar vigias (liberou o respiro mínimo) → cargo clean só do workspace (preservando o target caro do app em uso) → 22GB livres. MAS o disco do fundador segue ~95% (403GB de 460GB são dados dele). Duas lições de produto com evidência: (a) a "Automação de limpeza de disco com autorização do usuário" da lista Futuro do debriefing ACABA DE GANHAR caso real — o Lina deveria detectar pressão de disco e propor limpeza dos próprios artefatos; (b) sessões longas multi-terminal precisam de higiene de target/ (cargo clean periódico ou target compartilhado com GC). | sessão 2026-06-12 ~17h30: df 99%→95% pós-limpeza | **ALTA** (derruba o time; UX de produto) | aberto |

## Achado (2026-06-14, Terminal B/Ultra Code — F2-3-2 resize do terminal vivo)
- **Adicionar campo a `ProjectedNode` (core) é SEMPRE uma costura cross-crate**, porque o app tem um literal `ProjectedNode { … }` (helper de teste em `app/lina-gpui/src/inspector.rs:125`) SEM `..Default::default()`. Cada story que estende a projeção (cwd→rodada 360, stalled→F1-0-3, agora cols/rows/w/h→F2-3-2) paga a mesma costura manual de "+N campos None coordenada via Maestro". Os comentários "Costura <story>" no literal já são a cicatriz do padrão. Candidata barata: `#[derive(Default)]` em `ProjectedNode` + o fixture do inspector usando `..Default::default()` → adições de campo deixam de quebrar o app (o evento e o `apply` continuam explícitos). BAIXA (mecânico e previsível), mas adiciona 1 ponto de costura obrigatória a toda story de projeção. | sessão F2-3-2: `grep -rn "ProjectedNode {"` = só events.rs (real) + inspector.rs (fixture) | aberto

## Achado (2026-06-15, Terminal A — USO REAL: 3 apresentações p/ o fundador apresentar AO VIVO)
| 22c | **5ª ocorrência da família #22/#23, agora em SESSÃO DE USO REAL com 3 terminais simultâneos.** O fundador pediu 3 apresentações (decks HTML + guias) p/ uma reunião ao vivo. Despachei B (Operações), H (Pricing), I (Comercial) via `lina handoff` — entrega confirmada nos 3. Resultado: **nenhum produziu artefato**. B reportou `PRONTO:` com caminho de arquivo que NÃO existia no disco (falso-pronto, report sem evidência); H e I voltaram a `Idle` sem tocar nada. Pior: ao corrigir o B, o `lina ask` foi **bloqueado por `loop_detected`** (4 msgs ao mesmo nó em ~5min: handoff→pausa→re-handoff→ask) — o anti-loop do app me impediu de redirecionar um worker travado. Contornei abandonando o A2A e produzindo os 3 decks com **subagents próprios (Agent tool)** em paralelo, com verificação obrigatória de disco — entregaram os 3 (12/11/11 slides) + PDFs + hub index. Lições de produto (agora com impacto em CLIENTE real): (a) a família #22/#23 é o **gargalo nº 1 de confiabilidade da orquestração** — handoff confirmado ≠ trabalho feito; (b) `PRONTO` sem carimbo de evidência (hash/stat do artefato) deixa o orquestrador cego — o disco é a única verdade; (c) `loop_detected` é cego demais: não distingue cascata-tempestade de **correção legítima de um worker travado**. | sessão 2026-06-15 ~14h: 3 handoffs confirmados → 0 artefatos; `lina ask` a B = `loop_detected`; subagents cobriram 3/3 | **ALTA** (confiabilidade da orquestração + impacto em entrega real) | aberto |

## Achado (2026-06-16, Terminal B/Ultra Code — F3-0-1 SystemParams)
- **2ª família do flaky-em-paralelo (agora em `tests/history_f1_5_8.rs`):** `a_tail_paginado_byte_identico_nunca_excede` falha ~1 em 4 execuções quando o **arquivo de teste inteiro roda seus 6 testes em paralelo** (concorrência interna), mas passa **isolado 3/3** e não toca nenhum módulo fora de history/scrollback. Mesma assinatura do achado #57 (install/discovery), em outro módulo → é **sistêmico**: testes que usam tmpdir/estado de filesystem sob `cargo test` paralelo flakam. Custo concreto: ao validar a F3-0-1, a suíte completa piscou `FAILED 5/1` num teste totalmente alheio à minha fronteira (params.rs novo + enum Effort) — gastei uma rodada re-provando que não era regressão minha (isolado 3/3 + arquivo inteiro 1/4 reproduz sem meu diff). O **gate paralelo do Maestro vai piscar vermelho sem causa real** em qualquer story, não só nas que tocam install. Candidata: um `serial_test`/mutex nos testes que criam tmpdir, ou `--test-threads` por suíte de integração. MÉDIA (falso-positivo de regressão custa o turno do worker a cada validação). | sessão F3-0-1: `--test history_f1_5_8` repetido = 3 ok / 1 FAILED; teste-alvo isolado 3/3 ok; não referencia params/Effort | aberto

## Achado (2026-06-16, Terminal B/Ultra Code — F3-0-3 model/effort no spawn)
- **A fronteira de arquivos de uma story que ADICIONA CAMPO a um evento é SEMPRE maior que a declarada — inclui todo construtor literal, testes inclusos** (nova faceta do #61). A F3-0-3 declarou fronteira `events.rs`+`router.rs`+`agent_modal.rs`, mas adicionar `model`/`effort` a `DomainEvent::SpawnRequested` (serde-default não torna o campo opcional na CONSTRUÇÃO Rust, só na desserialização) quebrou a compilação em **`attention.rs`** (4 construtores em `#[cfg(test)]`) e **`bridge.rs`** (1 construtor de teste) — dois arquivos não previstos no despacho. Idem `RouteOutcome::SpawnApproved` (match em `bridge.rs:1027` sobreviveu só porque já usava `..`). Custo: o worker precisa caçar todos os construtores via `grep`/erros E0063 sucessivos (o compilador revela um lote por vez), e alguns caem em arquivos de OUTRO dono nominal. Mitiga: (a) o despacho de uma story "+campo em evento X" deveria listar `grep -rn "X {"` como fronteira REAL; (b) candidata estrutural — um construtor-helper/builder por evento (`SpawnRequested::new(...).with_model(...)`) centralizaria os defaults e tornaria campos aditivos não-quebradores nos testes. BAIXA-MÉDIA (mecânico, mas o sub-dimensionamento da fronteira gera edição em arquivo de peer e ruído de "quem é dono"). | sessão F3-0-3: E0063 em attention.rs (1303/1321/1371/1508) + bridge.rs:8506 após +2 campos em SpawnRequested | aberto

## Achado (2026-06-16, Terminal R — DESIGN: a "temperatura T3" da fusão F2 nunca foi implementada)
- **Decisão de design selada ≠ design implementado: a F2-0-D selou "fusão T1 + temperatura T3", mas só o T1 (instrumento) chegou ao código — a temperatura T3 (o calor do "Ateliê") ficou no protótipo `tasks/pesquisa-f2/prototipos/t3-atelie.html` e nunca virou token.** O fundador apontou: "nos showcases o design era COMPLETAMENTE DIFERENTE, um ateliê muito mais bonito, não tem NADA dele". Causa raiz confirmada no próprio `theme.rs` (comentário-confissão do módulo `scale`): *"O dark é a paleta histórica da Fase 0 (migrada, não redesenhada); o light foi desenhado token a token contra o gate WCAG"* — ou seja, o dark reciclou o azul-marinho FRIO da F0 e o light nasceu azulado/frio (`#f2f4fb`/`#fbfcff`), ambos contradizendo o papel quente do showcase (`#f4ede0`/`#fbf6ec`). Entraram do T1: fontes (Plex/Fraunces) + cor semântica de estado. NÃO entrou nada do T3: papel quente, terracota de identidade, ilhas dark quentes, caret âmbar, celebração contida. **Por que passou:** os commits diziam "identidade da fusão na tela" e os gates (WCAG, catraca, suíte) ficaram verdes — mas gate verde ≠ ficou bonito; o calor não tem teste automatizado, então foi a metade que caiu fora silenciosamente. **Lição de produto:** uma decisão estética selada precisa de um gate de TELA (olho humano contra o protótipo aprovado), não só de gates de contraste/número-mágico — senão "fusão" vira álibi para entregar só o esqueleto técnico. Correção aplicada nesta sessão (sob instrução do fundador, opção "fusão de verdade T1+T3"): paleta quente dark+light no `scale`, terracota como acento default (9 acentos), roxo-IA do `ACCENT_CREATE` banido→argila, caret/terminal âmbar — gate WCAG + catraca + suíte 515/14/2 + clippy -D + fmt todos verdes; visto na tela rodando (26fps, 6 terminais). Pendente (fatias seguintes): cor de identidade POR ESPAÇO (terracota é por-workspace no protótipo, hoje é global), selo de celebração contida, e avaliar fontes Atkinson/Bricolage do T3 puro vs Plex/Fraunces do T1. | sessão 2026-06-16: `theme.rs:205` comentário-confissão; `t3-atelie.html` vs `scale::CANVAS` antigo `0x0a0e27`/`0xf2f4fb` | **ALTA** (decisão selada entregue pela metade; impacto direto na percepção do fundador) | em correção

## Achado (2026-06-18, Terminal A — TESTE AO VIVO da rodada F3-1 Goal-and-Loop)

Teste de aceitação ao vivo do ciclo da Goal (define→interpret→confirm→seed) + card na tela. Meta de teste: `019edace-f162-7510-a731-2c9b7be2a07d`. Rebuild confirmado no ar (`lina goal status zzz` reconheceu o verbo; não voltou help geral).

| # | Achado | Evidência | Severidade | Status |
|---|---|---|---|---|
| GL-1 | **`lina plan seed <goal_id>` NÃO decompõe a meta — nenhum item de plano é criado.** A CLI envia `intent:"plan.seed"` (`to:"plan"`), mas o roteador despacha por allow-list de INTENT e `is_plan_intent` só conhece `plan.claim`/`plan.check` — `plan.seed`/`plan.add` faltam. Logo a msg não é interceptada, cai na resolução de alvo, `node_by_name("plan")`→None→`RouteBlocked{no_target}`, retentada ~65× até desistir. A lógica de decomposição (`GoalDecomposed`+`PlanItemAttributed`) existe DEFINIDA e com projeção, mas só é emitida em `#[cfg(test)]` — **não há caminho de produção**. O "decompõe em itens e persegue" do Goal-and-Loop não acontece pela CLI. (Efeito colateral: ~65 `RouteBlocked` poluindo o event log por seed.) `plan add` tem o mesmo bug. | CLI: `crates/lina-bootstrap/src/bin/lina.rs:1911-1914`. Allow-list: `crates/lina-core/src/router.rs:2522-2524` (vs `is_goal_intent` 2549-2551). `handle_plan` sem braço seed: `router.rs:1864-1884`. NoTarget: `router.rs:996-1011`. Emissão só em teste: `router.rs:7159` (mod tests começa 3072). Event log seq 6112-6176. | **ALTA** (bloqueia o ciclo de ponta a ponta) | **CORRIGIDO (Terminal A, esta sessão):** `is_plan_intent` passa a interceptar `plan.add`/`plan.seed`; `handle_plan` ramifica antes do `plan_ref()`; novos `handle_plan_seed` (decomposição determinística mínima — 1 item espelha a meta + herda critérios; gate de ciclo só `Confirmed`; ZERO LLM, inv #1) + `handle_plan_add` + helper `attribute_item`. 5 testes via `route_message` (caminho real, não monta-à-mão) + 388 lib verdes; clippy `-D`/fmt limpos. Pendente: validação E2E no app rebuildado. |
| GL-2 | **`--accept` (critérios de aceite) é descartado em define E interpret — nunca chega ao evento.** Descasamento de chave+forma no payload: a CLI serializa sob `"acceptance"` como array de OBJETOS `AcceptanceCriterion`; o handler lê `payload.get("accept")` esperando array de STRINGS. Resultado: `GoalInterpreted` sai com `acceptance_criteria:[]` mesmo passando `--accept`. Impacto: o critério — âncora do "persegue até o critério" e o que o card mostra em "Como vou saber que deu certo" — nunca existe; no card a seção de critérios some silenciosamente. | CLI emite `"acceptance"`: `lina.rs:1587`/`1687`. Handler lê `"accept"`/string: `router.rs:2281-2294`. Evento observado: `GoalInterpreted ... acceptance_criteria:[]` (log seq 6109). | **MÉDIA-ALTA** (esvazia a semântica do critério de aceite) | **CORRIGIDO (Terminal A, esta sessão):** `handle_goal` (`goal.interpret`) passa a ler a chave `acceptance` como `Vec<AcceptanceCriterion>` (mesmo contrato de `plan.add`), extraindo a `desc` e FORÇANDO `HumanReview` (preserva a invariante "Goal = HumanReview" — agente não injeta `Command` no nível da meta). Provado por `goal_interpret_persiste_acceptance_no_formato_da_cli`. Nota: `--accept` no `define` segue inerte por design (`GoalDefined` não tem campo de critério — eles pertencem à interpretação; não toquei `events.rs`/costura fixada). |
| GL-3 | **Card da Goal: affordance de confirmar pela UI é STUB** — botões "Sim, é isso"/"Quero ajustar" estão na tela e wired, mas `on_confirm`/`on_correct` só fazem `eprintln!`+`cx.notify()`; não emitem `GoalConfirmed`. Marcado `[SEAM: identidade do confirm]` (precisa de identidade de nó autenticada server-side). O gate humano pela TELA ainda não fecha o ciclo — só pela CLI. | `app/lina-gpui/src/main.rs:2147-2162` (stub) + `2129-2134` (wiring). | MÉDIA (gesto humano na UI ainda não persiste) | **RESOLVIDO (Terminal A, esta sessão):** `Router::human_intent` (5a999af, by=HUMAN_GESTURE server-side + red-team) + fila view→pump no `NodeManager` (3e0da40); `confirm_goal` enfileira `goal.confirm`, `correct_goal` abre editor inline que re-envia `goal.interpret` (0f08841). +UX pedida pelo fundador: cards **recolhíveis** (20aecd2) e **arrastáveis** (48c0385). 548 testes app verdes; clippy/fmt limpos. Validação do gesto AO VIVO = olho do fundador (rebuild). |
| GL-4 | **Positivo:** ciclo de fases `goal define→interpret→confirm` funciona e PERSISTE no event log (`GoalDefined`→`GoalInterpreted`→`GoalConfirmed`; projeção avança definida→interpretada→confirmada). Gate humano (confirm) muda a fase. `status` em pt-br claro, **zero jargão/erro**. **Replay determinístico ✓**: 2 processos `lina goal status` independentes deram projeção byte-idêntica, reconstruída do `log.jsonl` em disco (invariante #4 honrado). | log seq 6108-6110; diff de 2 projeções = idênticas. | — | evidência positiva |
| GL-5 | **Positivo:** card da Goal (`goal_card.rs`) é bem construído — render em produção (`main.rs:2106 render_goals_panel`), strings 100% pt-br com teste anti-vazamento de jargão (`goal_card.rs:546-575`), identidade própria (Fraunces hero vs IBM Plex corpo; cores por tokens semânticos; acento terracota, roxo-IA banido), e degrada graciosamente com `acceptance:[]`/`items:[]` (esconde critérios, barra "Começando" 0%, não quebra). Verificação a OLHO pendente (screencapture sem permissão no contexto do agente). | `app/lina-gpui/src/goal_card.rs` + `theme.rs`. | — | evidência positiva (falta olho humano) |


## Achado (2026-06-18, Terminal G — Rodada Confiabilidade, frente UI estado honesto)

- **#21 circuit_breaker é ESTRUTURALMENTE invisível no canvas — pior que jargão: é mentira.** O despacho (#21) descreveu "um terminal preso em `Blocked(circuit_breaker)` mostra jargão". Mapeando a trilha, a realidade na shell gpui é outra: o reason cru NUNCA chega ao app. `bridge.rs:2279` (`map_status`) COLAPSA `CoreStatus::Blocked → lina_host::NodeStatus::Busy`, e `lina_host::NodeStatus` (lib.rs) **nem tem a variante `Blocked` nem um campo `reason`**. Efeito: um nó pausado pelo disjuntor pinta como **âmbar "rodando"** no card (status_dot `main.rs:4164` + label) — o oposto de honesto. O jargão "circuit_breaker" que o fundador viu vaza pela **CLI** (`lina check` imprime "motivo: <reason>"), não pela tela do app. Logo a tela honesta do #21 **não é fechável só na fronteira da UI** (main.rs/goal_card.rs): exige B (W1) parar de colapsar Blocked→Busy e surfacar `Blocked{reason}` na projeção do bridge (ou rotear o breaker como item de atenção, à la W3) + um verbo de liberação no core p/ o "clique para liberar". Consumidor UI + copy ("pausado por segurança — clique para liberar") já prontos e testados (`main.rs` honest_state_tests), à espera do sinal. MÉDIA-ALTA (o estado mais crítico — "pausei por segurança" — é exibido como "trabalhando"; o fundador não tem como saber que o time travou). | sessão Rodada-Confiabilidade: `bridge.rs:2279` colapsa Blocked→Busy; `lina-host/src/lib.rs` NodeStatus sem Blocked/reason; jargão real do reason via CLI `lina check` | **escalado ao Maestro (Terminal A)** — pende fronteira de B/W1 + verbo core
- **Positivo (#22):** o contrato de W3 (`AttentionKind::DeliveredNoProgress`, sem `#[non_exhaustive]`) fez o compilador **forçar** a integração UI: o variant novo deixou `attention_ui.rs` RED até a UI tratá-lo — impossível engolir o estado novo num `_ => {}`. Costura auto-anunciante; integrei badge no card + toast/painel/selo "aviso" + `is_goto_only` (não-aprovável). É o padrão certo p/ estados de observabilidade novos.
- **Positivo (#22, achado pervasivo):** o card mostrava `· {:?} · {:?}` (Debug cru do enum) = "· Terminal · Busy"/"· Terminal · Idle" — jargão em INGLÊS em TODO card, enquanto a a11y (`a11y::node_label`→`aggregate_badge`) já falava pt-br. Assimetria visual-vs-a11y clássica (alguém traduziu a camada invisível e esqueceu a visível). Corrigido p/ a MESMA fonte da a11y (`canvas::aggregate_badge(...).label()`) — visual e leitor de tela deixam de divergir. Lição: ao auditar "tela honesta", grep por `{:?}` em `text!`/`format!` de render pega vazamento de Debug que nenhum gate de cor/contraste/WCAG enxerga.


## Achado (2026-06-18, Terminal I — F3-2 frente F2BRIEF: briefing + #15 `lina history`)

- **Teste FLAKY em paralelo: `history_f1_5_8::d_export_json_round_trip` falha intermitente com `Sqlite("duplicate column name: ts")`.** O `TempDir` desse teste (`crates/lina-core/tests/history_f1_5_8.rs:14-38`) qualifica a pasta só por `process::id()` + `SystemTime::now().nanos` — SEM `thread::current().id()`. Sob `cargo test` paralelo, dois testes do MESMO processo podem cunhar o mesmo nanos → mesma pasta → o mesmo `scrollback.db` é aberto 2× e a migração de schema roda em cima de si mesma → "duplicate column name". Passa 6/6 isolado/serial (`--test-threads=1`). Correção trivial: incluir `thread::current().id()` no nome do tempdir (o `TempDir` de `attention.rs` e os meus testes do bin já fazem). MÉDIA-BAIXA (não é bug de produção; é falso-vermelho que envenena o gate paralelo e mascara regressão real). Arquivo de `tests/` = fronteira do R nesta rodada — sinalizado, não corrigido. | `crates/lina-core/tests/history_f1_5_8.rs:14-38` | **sinalizado ao Maestro**
- **Observação (coordenação):** durante a validação, a suíte `lina-core` ficou RED por COMPILE ERROR transitório em `router.rs:2600,2602` (`proposed_team_of`/`spawn_id_exists` não encontradas — F3-2-3 do CORE/B em andamento na working tree compartilhada). Provei minha fatia (briefing.rs + lina.rs) verde em **git worktree isolado no HEAD** (router.rs committado/são): clippy `-D` 0 + briefing 6/6 + history 4/4. Padrão útil p/ desacoplar a validação da própria fatia do WIP de peer numa árvore compartilhada.

## Achado (2026-06-18, Terminal R — F3-2 frente F2QA: segurança do loop + adoção + repro #22/#23)

- **Positivo (segurança, F3-2-3/B):** a montagem de time (`assemble_team`, `router.rs:2601-2614,2632`) separa o GATE do GASTO de forma exemplar: a confirmação da Goal por um AGENTE **registra** o fato (`GoalConfirmed`, `by` server-side, auditável) mas **NÃO dispara a montagem** — em Assisted/manual a `assemble_team` só roda com `HUMAN_GESTURE` (toque da UI) ou `autonomo`. "Registrar o fato ≠ autorizar a ação gated" fecha a porta da inteligência se auto-autorizando a gastar recurso. `requested_by`/`root_cause_id`/`hops:0` carimbados server-side, nunca do `proposed_team`. Provado por QA por CONTRASTE (mesmo cenário: agente→0 spawns, gesto humano→N spawns) em `tests/f3_2_seguranca_loop.rs`. Padrão a preservar nas próximas ondas. | gate (g)/(c) F3-2 verde, 0 ALTA
- **Não-duplicação (QA):** `f3_1_goal_adversarial.rs` já cobria 3 das 4 invariantes do despacho F2QA (`GoalConfirmed.by` server-side, `reviewer != target` no caminho real, `ReviewVerdict{Pass}` ⇏ `GatedHard`). O valor da rodada foi cobrir o GAP do caminho NOVO (interpretação=dado, `acceptance`→HumanReview, montagem gated) — ler o que já existe antes de escrever evita re-provar o coberto. `acceptance`→HumanReview provado por MUTAÇÃO em worktree isolado (RED com a guarda desligada). | `tests/f3_2_seguranca_loop.rs` (6) + `tests/f3_2_adocao.rs` (métrica; adoção viva=0%) + `tests/repro_22_23_residual.rs` (3) + `tasks/epico-f3/repro-22-23.md`
- **Positivo (#15):** a API core de leitura cross (`history::tail_cross`/`search_cross`/`export_cross`) já vinha com a fronteira de pertencimento (ADR 0006) + auditoria-antes-de-ler (`HistoryReadCross`, falha-ao-auditar-NEGA) PRONTAS e testadas — a fiação do verbo no bin foi só resolver identidade (env ADR 0026, nunca flag) + membros (projeção do log) + painel (NodeId, como o app chaveia) e chamar `*_cross`. Contrato bem desenhado puxa fiação curta e segura.

## Achado (2026-06-19, Terminal A — Maestro da rodada Confiabilidade v2)
| 26 | **`permission_prompt` de um worker vira gargalo invisível ao orquestrador.** Na largada da F3-CONF-2, o Terminal I (frente HARNESS) saiu de Busy para `Blocked (motivo: permission_prompt)` ao rodar a validação (`cargo test` repetido). O Maestro **não pode aprovar pelo worker** — a aprovação é gesto humano custodiado (ADR 0021/0025), correto por segurança. Mas o orquestrador fica cego ao CONTEÚDO do prompt (`lina check` só diz `permission_prompt`, não QUAL comando): não dá para narrar ao leigo "aprove o `cargo test`" com precisão, só "tem um pedido na tela". E `Blocked(permission_prompt)` não corre relógio de stall (correto, ADR 0019) — então um worker pode ficar parado indefinidamente esperando um humano que "seguiu o dia". Candidata de produto: (a) `lina check` surfacar o COMANDO pendente do permission_prompt (mesma família do circuit_breaker legível #21 — o orquestrador precisa ver o que o worker espera); (b) numa rodada multi-terminal, o Maestro deveria poder agregar os prompts pendentes ("3 terminais esperam aprovar `cargo test` — liberar todos?") em vez de o humano caçar 1 a 1 na tela. Distinto do #22/#23 (lá a msg some; aqui o worker PARA num gate legítimo, mas mudo ao orquestrador). | sessão 2026-06-19: `lina check "@Terminal I"` = Blocked/permission_prompt logo após o handoff; B/G/R seguem Busy (não-sistêmico nesta largada) | MEDIA (observabilidade do orquestrador + UX de aprovação em lote) | aberto |
| 27 | **`lina history @colega` rejeita o `LINA_NODE_ID` do próprio Maestro (com prefixo `n-`) — o verbo de observabilidade #15 (entregue na F3-2) quebra na prática.** Ao monitorar o trajeto do Terminal G (passo 6 do loop), `lina history "@Terminal G" --tail 30` falhou com `lina: LINA_NODE_ID invalido: "n-019edde3-faa9-7c51-bd5a-9f53c18addc0"`. O `reader_node_id()` (bin lina.rs) parseia o `LINA_NODE_ID` do env e rejeita o formato `n-<uuid>` que o spawn carimba — então o Maestro NÃO consegue "ver a tela do worker", que era exatamente o gap #15 que a F3-2 fechou. Contornei lendo o DISCO (git diff dos arquivos do worker na árvore compartilhada), que provou ser a fonte de verdade mais confiável de qualquer forma (a evidência no disco > o relato — mesma lição #22c). Mas o caminho OFICIAL de observabilidade do orquestrador está mudo. Candidata: `reader_node_id()` aceitar/normalizar o prefixo `n-` (ou o NodeId::parse tolerá-lo), com teste de paridade contra o formato que o spawn de fato carimba. | sessão 2026-06-19: `lina history "@Terminal G"` exit≠0 com a msg acima; `git diff` do worker funcionou como fallback | MEDIA-ALTA (a observabilidade do Maestro que a F3-2 entregou não funciona no caminho real do spawn) | aberto |
| 28 | **A família #22/#23 NÃO se manifestou nesta largada de 4 frentes — evidência positiva do fix de confiabilidade já no ar.** Os 4 workers (B/G/R/I) reportaram `comecei` e transicionaram a Busy sem nenhum despacho engolido; os reportes de `PRONTO` chegaram (alguns com atraso, mas chegaram). Contraste direto com 2026-06-15 (#22c: 3 handoffs → 0 artefatos). O protocolo MANUAL do Maestro (confirmar 1º progresso por Busy real + validar pela evidência no disco, não pelo ack) cobriu o resíduo — e é exatamente o que o CONF-CORE (c113f36) acabou de MECANIZAR para o caso goal-driven. A rodada que conserta o #22/#23 rodou sobre um #22/#23 já domado. | sessão 2026-06-19: 4/4 Busy confirmado, 0 engolido, fatias validadas pelo disco | — (evidência p/ o gate de confiabilidade) | registro |

## Sessão 2026-06-20/21 (Terminal A — melhorias de dogfooding pedidas pelo fundador na tela)

> Cinco atritos vividos pelo fundador usando o app (criar/editar terminal, religar o CLI). Todos FECHADOS em código com gate verde (561 testes do app + 420 do core) e o app relançado. ADRs 0037 (cwd por-nó) e 0039 (trocar motor). NÃO commitado ainda (há trabalho de peer não-salvo no `bridge.rs` — o kit-lina/ADR 0038; o commit espera não pisar nele).

| # | Achado | Evidência | Severidade | Status |
|---|---|---|---|---|
| 29 | **Editar a "Pasta de trabalho" de um terminal era um clique decorativo**: o campo + botão "Trocar…" apareciam no modo Editar, mas o valor era DESCARTADO ao salvar (`SavePlan` sem campo `cwd`; `new_edit` não pré-carregava o cwd real) — o terminal não ia para a pasta nova e a mudança não sobrevivia ao reabrir (voltava à pasta de criação). | tela do fundador 2026-06-20; `agent_modal.rs` (SavePlan sem cwd) + `main.rs` commit só fazia rename/role | MEDIA (UX core: a edição mente) | **FECHADO** — evento aditivo `NodeCwdSet` + modal pré-carrega/persiste + "reiniciar agora" re-ergue na nova pasta (`restart_node_in_dir`). ADR 0037. Testes: `node_cwd_set_*`, `edit_mode_save_plan_detects_cwd_change`, `restart_node_in_dir_*` |
| 30 | **Terminal-com-Claude não religava o CLI ao reabrir** (queixa original): o boot TENTA re-spawnar o `claude` direto, mas o `program` é o nome nu `claude` — no boot via Dock o PATH é mínimo e o exec falha → o restore degrada para shell puro (badge `EngineMissing`) e o usuário digita `claude` à mão. | queixa do fundador; `bridge.rs` restore reconstrói command do profile (nome nu); `plan_restore`/`restore_terminals` | MEDIA | **FECHADO** (junto do #31) — `resolve_cli_program` resolve o binário p/ caminho absoluto no restore (dirs canônicos + nvm), `absolutize_restore_commands` nos 2 caminhos de restore. ADR 0037 §âncora |
| 31 | **Motores Claude/Antigravity/OpenCode SOMEM do modal — só Codex e Gemini aparecem** (causa raiz da queixa original): o app via Dock monta um PATH com o Homebrew (codex/gemini em `/opt/homebrew/bin`) mas SEM `~/.local/bin` (claude/agy) nem `~/.opencode/bin` → a descoberta não os acha. **A 1ª tentativa de fix falhou** porque a função que garante esses dirs tinha um `return` no topo (`path_looks_hydrated`) que a pulava quando o Homebrew já estava no PATH. Diagnóstico por REPRODUÇÃO empírica (teste com o PATH real provou: a busca acha os 5 motores quando `~/.local/bin` está presente — o defeito era só o PATH nunca recebê-lo). | screenshot 2026-06-20 18:58 (só Codex/Gemini); teste `temp_debug_discover_real_path` (removido) achou 5/5; `which claude` = `~/.local/bin/claude` | **ALTA** (sem o claude descoberto, todo o fluxo desmorona) | **FECHADO** — `ensure_known_cli_dirs_in_path` SEMPRE garante os dirs de CLI (+`~/.opencode/bin`), fora do gate `path_looks_hydrated`. Teste de regressão `missing_cli_dirs_includes_local_bin_even_with_homebrew_present` |
| 32 | **Criar terminal não abria o modal de configuração**: o botão "➕ Terminal" criava um shell puro DIRETO (sem escolher motor/pasta) — o fundador esperava configurar. (A porta RICA `✦ Novo Agente`/⌘N existia, mas a do `➕` pulava o modal.) | tela do fundador 2026-06-20; `main.rs` `add-terminal-btn` → `add_node()` | BAIXA-MEDIA (UX de onboarding: o leigo não distingue Terminal de Agente) | **FECHADO** — o botão "➕ Novo Terminal" abre `open_agent_modal_create`; ⌘T segue como atalho rápido p/ avançados |
| 33 | **Trocar o motor na janela de Editar estava marcado "em breve"** (`COPY_EDIT_ENGINE_LOCKED`): os chips de motor eram clicáveis, mas a troca era descartada (`SavePlan` sem motor) — o fundador selecionava outro CLI e nada acontecia. | screenshot 2026-06-20 (aviso "⚠ trocar o motor reinicia o Agente — em breve") | MEDIA | **FECHADO** — `SavePlan.engine` + pré-seleção do motor atual (`set_engines`) sem troca-falsa + `restart_node_in_dir` com `engine_override` re-ergue com o novo CLI; aviso honesto só quando o motor muda. ADR 0039. Teste `edit_mode_save_plan_detects_engine_change` |

## Sessão 2026-06-21 (Terminal G — M-UI da F3-3 Mentality, painel "Como o [papel] pensa")

| # | Achado | Evidência | Severidade | Status |
|---|---|---|---|---|
| 34 | **A projeção `Mentality` (M-PROMO) não expõe enumeração de papéis — o consumidor de UI fica sem como listar "quais papéis aprenderam algo".** A API é toda por-papel (`beliefs_for_role`/`top_k_for_role`) e o campo `beliefs` é privado; `DomainEvent::from_record` é `pub(crate)`, então o app NÃO pode decodificar `BeliefProposed` para extrair os papéis. Contornei enumerando os papéis pelo ROSTER VIVO (`NodeManager::cards`→`node_role`) + `top_k_for_role` por papel — funciona p/ o gate (h) (o papel corrigido tem nó vivo na tela), mas um papel que aprendeu numa sessão passada e não tem nó vivo AGORA não aparece no painel. Candidata: `Mentality` expor `pub fn roles(&self) -> Vec<String>` (ou `all_beliefs(&self) -> Vec<&Belief>`) — o adaptador `Belief→BeliefView` passa a iterar a projeção direto (1 função muda). Alinhar com o I (dono da projeção). | `mentality.rs` (sem `roles()`; `beliefs` privado); `events.rs:1316` `pub(crate) fn from_record`; `main.rs::refresh_mentality_cache` (enum por roster) | MEDIA (cobertura do painel: papel-sem-nó-vivo fica invisível; o desacoplamento via view-model mantém o custo do fix em 1 fn) | aberto (proposto ao Maestro/I) |
| 35 | **`lina plan claim <ID>` responde "enfileirado" mas o `@owner` não reflete no `lina plan read` mesmo re-tentando — o claim fica invisível ao orquestrador.** Dei `lina plan claim F33-UI` 2× (ambas "ok: enfileirado"), mas `plan read` seguiu `@owner:?` por minutos — o Maestro (A) avisou "não vi claim". O gesto é assíncrono (supervisor drena), mas sem o owner refletido o board não conta quem assumiu o quê (a "fonte da verdade da coordenação" do CLAUDE.md). Distinto do #22/#23 (lá a MSG some); aqui o EVENTO de claim parece enfileirado-mas-não-drenado. O trabalho seguiu (código + gates verdes), mas a coordenação por plano ficou muda. Candidata: o claim refletir rápido (ou `lina plan claim` confirmar a aplicação, não só o enfileiramento). | sessão 2026-06-21: 2× `enfileirado`, `plan read` = `@owner:?` por minutos | MEDIA (observabilidade da coordenação — o board não vê o dono) | aberto |
